# Tiny smoke matrix to verify the full pipeline end-to-end (P5).
# Deliberately small: a few functions/dims/instances at reduced budget.
suite_name: bbob
models: [gp, bnn_mc_dropout, pfn_bnn, pfn_hebo, gmm_np, pfn_transformer]
ecs: [lmm, dts, lq]
functions: "1,8,15,21"     # separable / low-cond / multimodal / weak-structure
dimensions: [2, 3]
instances: "1-3"
budget_mult: 50            # reduced from 250 just for the smoke test
seed: 42
exdata_dir: exdata
diag_dir: results/diagnostics
