# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# macOS / zip cruft
.DS_Store
__MACOSX/

# Experiment outputs (large, regenerable)
results/
exdata/
*.tar.gz
ppdata/
*_cocopp/

# Model weights are committed (2 MB) so Colab/CI need no pretraining step.
# (intentionally NOT ignored: models/models/weights/pfn_bnn_transformer.pt)

# Large / regenerable doc binaries (markdown summaries are kept)
docs/extracted-pdfs/
docs/*.pdf
docs/*.zip

# Editors
.vscode/
.idea/
