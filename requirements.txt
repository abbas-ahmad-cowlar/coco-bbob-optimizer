# Surrogate-Assisted CMA-ES on COCO/BBOB — Python dependencies
# Verified working on Python 3.14 (Windows, MSVC build tools present for coco-experiment).

# Core scientific stack.
# Lower bounds chosen so `pip install` does NOT force-upgrade Colab's preinstalled
# numpy/torch (an ABI break) — these are satisfied by both local and Colab stacks.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pandas>=1.5
matplotlib>=3.7
seaborn>=0.12
statsmodels>=0.14          # Holm correction (multipletests)
pyyaml>=6.0                # experiment configs + transformer arch config

# Optimization engine + COCO benchmarking
cma>=4.0                   # CMA-ES / IPOP-CMA-ES base optimizer
coco-experiment>=2.8       # cocoex: BBOB suite + COCO-format logging
cocopp>=2.8                # COCO post-processing: ECDF, baseline archives

# Neural surrogate models (GP uses sklearn; the rest use torch)
torch>=2.0                 # CPU build is sufficient; CUDA optional for the full run
